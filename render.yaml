services:
  - type: web
    name: georef-logistics
    env: node
    buildCommand: npm install && npm run client:install && npm run client:build
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: MONGODB_URI
        sync: false
      - key: JWT_SECRET
        sync: false
